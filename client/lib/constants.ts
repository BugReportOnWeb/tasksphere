import { UpdateTaskReq }  from '../types/Task';

const sampleTask: UpdateTaskReq = {
    title: "Random title",
    description: "Some random descriptiong for random title (testing purpose)",
    completed: false
}

export { sampleTask };
